import React from "react";
 
export const  Home = () => {
    //const [count, setCount] = useState(0);

    // useEffect(() => {
    //   document.title = `You clicked ${count} times`;
    // });

    return(
        <h1>Home</h1>
    );
}